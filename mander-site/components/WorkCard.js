// Deprecated. Selected Work moved to the stacked, alternating editorial
// layout — see WorkCase.js — as part of the Uniqlo/MUJI/MUBI/Linear rebuild.
// Kept as a re-export so any stray import still resolves.
export { default } from './WorkCase';
