// Deprecated. The glassy tile was replaced by the editorial ServiceBand in the
// Uniqlo-style rebuild. Kept as a re-export so any stray import still resolves.
export { default } from './ServiceBand';
